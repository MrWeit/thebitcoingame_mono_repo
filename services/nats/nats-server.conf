# TheBitcoinGame — NATS Server Configuration (Phase 4)
# Single-node JetStream for cross-region event replication

# Client connections
listen: 0.0.0.0:4222

# HTTP monitoring
http_port: 8222

# Server name
server_name: tbg-nats

# JetStream configuration
jetstream {
    store_dir: /data/jetstream
    max_mem: 256MB
    max_file: 1GB
}

# Logging
debug: false
trace: false
logtime: true

# Limits
max_payload: 1MB
max_connections: 256
